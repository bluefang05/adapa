# ADAPA Flutter v0.7

This iteration turns the Dart/Flutter content engine into a complete Android
project layout ready to be opened and built by Flutter.

Completed:

- real `android/` host project;
- package `com.enmanuelapps.adapa`;
- current Flutter 3.44.x-compatible Gradle template;
- minSdk 24;
- Java 17;
- AndroidX;
- Android 11+ TTS query;
- release manifest remains offline;
- debug/profile INTERNET permission only;
- launcher resources;
- local.properties example;
- key.properties example;
- current `shared_preferences` dependency;
- static Android project audit.

Not completed yet:

- actual `flutter analyze` / `flutter test`;
- APK/AAB compilation;
- real Play upload signing;
- polished branding/icon;
- UI/UX polish.
