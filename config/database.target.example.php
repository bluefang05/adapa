<?php

// Copia este archivo como `database.target.php` para decidir desde localhost
// si quieres usar la base local o la remota.
//
// Valores permitidos:
// - 'local'
// - 'remote'
// - 'auto'
//
// En hosting, `auto` sigue usando la remota si existe `database.hosting.php`.
// En localhost, `auto` usa la base local por defecto.

return 'local';
